# Homebrew tap for Appear

Install [Appear](https://github.com/JackUait/appear) — summon any app with a keyboard shortcut:

```bash
brew install --cask jackuait/appear/appear
```

That's it: one trusted command, no Gatekeeper warnings (the cask clears the
download quarantine on install).
